{
  "version": "0.2.0",
  "name": "dsh-opencode-zen",
  "dsh": {
    "bundle": {
      "patch": "./cordis.patch.yml"
    },
    "client": {
      "platform": "web"
    },
    "id": "opencode-zen",
    "changelog": {
      "0.1.0": "Initial release: 7 free models from OpenCode Zen, zero config",
      "0.2.0": "Add MiniMax M1 Lite/Scan models to fallback; persistent file cache; retry with exponential backoff; improved error messages"
    }
  },
  "description": "DSH plugin: brings OpenCode Zen's free models to DeepSeek Harness — zero config, no API key required, 9 free models with tool calls and reasoning support",
  "scripts": {
    "build": "tsdown",
    "check": "tsc --noEmit",
    "test": "node --test tests/*.test.ts"
  },
  "type": "module",
  "main": "lib/index.js",
  "files": [
    "lib/",
    "cordis.patch.yml",
    "README.md",
    "README.zh-CN.md",
    "LICENSE"
  ],
  "author": "Science Roam Limited (forked and optimized by randomix777)",
  "keywords": [
    "ai",
    "llm",
    "dsh",
    "deepseek-harness",
    "opencode",
    "zen",
    "free-models"
  ],
  "categories": [
    "ai",
    "productivity"
  ],
  "license": "MIT",
  "peerDependencies": {
    "@deepseek-ai/cordis": ">=4.0.0 <5",
    "@deepseek-ai/dsh-llm": ">=1.0.0"
  },
  "peerDependenciesMeta": {
    "@deepseek-ai/cordis": {
      "optional": true
    },
    "@deepseek-ai/dsh-llm": {
      "optional": true
    }
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsdown": "^0.15.6",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=20"
  }
}
